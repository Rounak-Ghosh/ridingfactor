<!DOCTYPE html>
<html>
  <head>
    <title>XFACTOR</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"/>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css"/>
    <style>
      body {
        padding-top: 70px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="jumbotron">
        <h1 class="text-primary text-center">
          <% if (user.isAdmin === 'true') { %>
            <span class="fa fa-user"></span> Admin Information
          <% } else { %>
            <span class="fa fa-user"></span> Profile Information
          <% } %>
        </h1>
        <div class="row">
          <div class="col-sm-6">
            <div class="well">
              <div>
                <img src="<%= user.photos[0].value %>" alt="profile" />
              </div>
              <p>
                <strong>Account Id</strong>: <%= user.id %><br />
                <strong>Email</strong>: <%= user.emails[0].value %><br />
                <strong>Name</strong>: <%= user.displayName %><br />
                <% if (user.isAdmin === 'true') { %>                      <!-- admin conditional statements -->
                    <strong>Admin Access</strong>: <%= user.isAdmin %><br />
                <% } %>
            </p>
            </div>
            <div>
              <a href="/auth/google/signout" class="btn btn-danger"
                ><span class="fa fa-user"></span> Sign Out</a
              >
            </div>
          </div>
        </div>
      </div>  
    </div>
  </body>
</html>
